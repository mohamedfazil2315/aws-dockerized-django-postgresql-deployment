# aws-dockerized-django-postgresql-deployment
Designed and deployed a production-ready two-tier web application architecture on AWS using Dockerized Django and PostgreSQL containers hosted on separate EC2 instances.
